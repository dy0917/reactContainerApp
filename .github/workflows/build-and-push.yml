name: Build and Push to ACR
on:
  push:
    branches:
      - 'main'
jobs:
  build:
    name: 'Build and Push to ACR'
    runs-on: ubuntu-latest

    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Docker Login
        uses: azure/docker-login@v1
        with:
          login-server: caf2c0c9c8eeacr.azurecr.io
          username: oj2uzFJsHl
          password: lwXvy5sGOt3KoF0ZyqVch25yMk0S4fb+ACRDUj7By

      - name: Build and Push to ACR
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: 2.0.0
          file: Dockerfile